import { SortArticlesPipe } from './sortArticles.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortArticlesPipe();
    expect(pipe).toBeTruthy();
  });
});
